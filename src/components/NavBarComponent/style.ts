import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    block: {
        width: '100%',
        height: '100%',
        backgroundColor: '#fff'
    }
})

export default styles