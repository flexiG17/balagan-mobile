import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    block: {
        display: 'flex',
        gap: 10
    },
    text: {
        textAlign: "center"
    }
})

export default styles