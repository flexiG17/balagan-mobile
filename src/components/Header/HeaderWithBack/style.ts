import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    titleBlock: {
        flexDirection: 'row',
    },
    title: {
        textAlign: 'center',
        fontSize: 18,
        fontWeight: '800',
    },
    titlePosition: {
        marginLeft: 'auto',
        marginRight: 'auto'
    },
})

export default styles