import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    grid: {
        paddingLeft: 25,
        paddingTop: 80,
        paddingRight: 25,
        paddingBottom: 45
    }
})

export default styles