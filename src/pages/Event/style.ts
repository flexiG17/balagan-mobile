import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    background: {
        top: 0,
        minHeight: 1000,
        width: '100%',
        height: '100%'
    },
})

export default styles