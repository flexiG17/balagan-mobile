import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
    background: {
        top: 0,
        width: '100%',
        minHeight: 1000,
        height: '100%',
    },
})

export default styles